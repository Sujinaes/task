## Project Overview

This project demonstrates a complete CI/CD pipeline for a containerized web application consisting of a frontend, backend, and database. Infrastructure is provisioned using Terraform, Docker images are stored in Amazon ECR, and deployments to an EC2 instance are automated through GitHub Actions.